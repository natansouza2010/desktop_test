version: '3.8'

services:
  desktop_test:
    container_name: desktop_test
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=desktop_test